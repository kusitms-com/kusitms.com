/** enum position FRONTEND BACKEND PLAN DESIGNER */
export enum BlogPosition {
  FRONTEND = "FRONTEND",
  BACKEND = "BACKEND",
  PLAN = "PLAN",
  DESIGNER = "DESIGNER",
}

/** enum category DOCUMENT INTERVIEW GIFT MEETUP GROUP_TF */
export enum BlogCategory {
  DOCUMENT = "DOCUMENT",
  INTERVIEW = "INTERVIEW",
  GIFT = "GIFT",
  MEETUP = "MEETUP",
  GROUP_TF = "GROUP_TF",
}
